apiVersion: v1
kind: Namespace
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: klap
    control-plane: controller-manager
  name: klap-system
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: entries.klap.genesary.github.com
spec:
  group: klap.genesary.github.com
  names:
    kind: Entry
    listKind: EntryList
    plural: entries
    singular: entry
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.dn
      name: DN
      type: string
    - jsonPath: .spec.serverRef.name
      name: SERVER
      type: string
    - jsonPath: .status.conditions[?(@.type=="Available")].status
      name: AVAILABLE
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Entry is the Schema for the entries API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Entry
            properties:
              adopt:
                default: true
                description: Adopt indicates if pre-existing entry should be adopted
                  or not
                type: boolean
              attributes:
                additionalProperties:
                  items:
                    type: string
                  type: array
                description: Attributes associated to the entry depends its classes
                type: object
              dn:
                description: DN (distinguished name) is a unique identifier that pinpoints
                  the entry in an LDAP directory
                type: string
              force:
                default: false
                description: Force indicates if entry modification should be forced
                  even if it leads to data loss
                type: boolean
              prune:
                default: true
                description: Prune indicates if entry should be delete or not
                type: boolean
              serverRef:
                description: ServerRef is a reference to a server configuration
                properties:
                  name:
                    description: Name of a resource
                    type: string
                  namespace:
                    description: Namespace of a resource
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - adopt
            - attributes
            - dn
            - force
            - prune
            - serverRef
            type: object
          status:
            description: status defines the observed state of Entry
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the Entry resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              guid:
                description: GUID is the global unique identifier of the remote entry.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: servers.klap.genesary.github.com
spec:
  group: klap.genesary.github.com
  names:
    kind: Server
    listKind: ServerList
    plural: servers
    singular: server
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.url
      name: URL
      type: string
    - jsonPath: .spec.baseDN
      name: BASE_DN
      type: string
    - jsonPath: .spec.implementation
      name: IMPLEMENTATION
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Server is the Schema for the servers API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Server
            properties:
              allowedNamespaces:
                description: |-
                  AllowedNamespaces restricts which namespaces' Entries are allowed to use
                  this Server. When omitted, only Entries from the Server's own namespace may
                  reference it. An Entry located in the same namespace as the Server is always
                  allowed, regardless of this selector.
                properties:
                  labelSelector:
                    description: |-
                      LabelSelector selects namespaces by their labels. When set, any Entry whose
                      namespace carries labels matching the selector is allowed to use the Server.
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                  namePattern:
                    description: |-
                      NamePattern is a regular expression matched against the full name of the
                      Entry's namespace. When set, any Entry whose namespace name matches the
                      pattern is allowed to use the Server.
                    type: string
                type: object
              baseDN:
                description: BaseDN is the base distinguished name for the server
                type: string
              bindDN:
                description: BindDN is the distinguished name to bind as when connecting
                  to the server
                type: string
              implementation:
                default: openldap
                description: Implementation is the server implementation type (e.g.,
                  "openldap", "activedirectory")
                type: string
              passwordSecretRef:
                description: PasswordSecretRef is a reference to a secret containing
                  the password to bind with
                properties:
                  key:
                    description: Key within a secret
                    type: string
                  name:
                    description: Name of a secret
                    type: string
                required:
                - key
                - name
                type: object
              startTLS:
                default: false
                description: StartTLS indicates whether to use StartTLS when connecting
                  to the server
                type: boolean
              timeout:
                default: 30s
                description: |-
                  Timeout is the maximum duration for any operation against the server. When
                  omitted, a default timeout of 30 seconds is used.
                type: string
              tlsSecretRef:
                description: TlsSecretRef is a reference to a secret containing TLS
                  configuration for the server
                properties:
                  key:
                    description: Key within a secret
                    type: string
                  name:
                    description: Name of a secret
                    type: string
                required:
                - key
                - name
                type: object
              url:
                description: Url is the URL of the server
                type: string
            required:
            - baseDN
            - bindDN
            - implementation
            - passwordSecretRef
            - startTLS
            - timeout
            - url
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources: {}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: klap
  name: klap-controller-manager
  namespace: klap-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: klap
  name: klap-leader-election-role
  namespace: klap-system
rules:
- apiGroups:
  - ""
  resources:
  - configmaps
  verbs:
  - get
  - list
  - watch
  - create
  - update
  - patch
  - delete
- apiGroups:
  - coordination.k8s.io
  resources:
  - leases
  verbs:
  - get
  - list
  - watch
  - create
  - update
  - patch
  - delete
- apiGroups:
  - ""
  resources:
  - events
  verbs:
  - create
  - patch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: klap
  name: klap-entry-admin-role
rules:
- apiGroups:
  - klap.genesary.github.com
  resources:
  - entries
  verbs:
  - '*'
- apiGroups:
  - klap.genesary.github.com
  resources:
  - entries/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: klap
  name: klap-entry-editor-role
rules:
- apiGroups:
  - klap.genesary.github.com
  resources:
  - entries
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - klap.genesary.github.com
  resources:
  - entries/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: klap
  name: klap-entry-viewer-role
rules:
- apiGroups:
  - klap.genesary.github.com
  resources:
  - entries
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - klap.genesary.github.com
  resources:
  - entries/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: klap-manager-role
rules:
- apiGroups:
  - ""
  resources:
  - namespaces
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - ""
  resources:
  - secrets
  verbs:
  - get
- apiGroups:
  - events.k8s.io
  resources:
  - events
  verbs:
  - create
  - patch
- apiGroups:
  - klap.genesary.github.com
  resources:
  - entries
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - klap.genesary.github.com
  resources:
  - entries/finalizers
  verbs:
  - update
- apiGroups:
  - klap.genesary.github.com
  resources:
  - entries/status
  verbs:
  - get
  - patch
  - update
- apiGroups:
  - klap.genesary.github.com
  resources:
  - servers
  verbs:
  - get
  - list
  - watch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: klap-metrics-auth-role
rules:
- apiGroups:
  - authentication.k8s.io
  resources:
  - tokenreviews
  verbs:
  - create
- apiGroups:
  - authorization.k8s.io
  resources:
  - subjectaccessreviews
  verbs:
  - create
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: klap-metrics-reader
rules:
- nonResourceURLs:
  - /metrics
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: klap
  name: klap-server-admin-role
rules:
- apiGroups:
  - klap.genesary.github.com
  resources:
  - servers
  verbs:
  - '*'
- apiGroups:
  - klap.genesary.github.com
  resources:
  - servers/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: klap
  name: klap-server-editor-role
rules:
- apiGroups:
  - klap.genesary.github.com
  resources:
  - servers
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - klap.genesary.github.com
  resources:
  - servers/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: klap
  name: klap-server-viewer-role
rules:
- apiGroups:
  - klap.genesary.github.com
  resources:
  - servers
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - klap.genesary.github.com
  resources:
  - servers/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: klap
  name: klap-leader-election-rolebinding
  namespace: klap-system
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: klap-leader-election-role
subjects:
- kind: ServiceAccount
  name: klap-controller-manager
  namespace: klap-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: klap
  name: klap-manager-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: klap-manager-role
subjects:
- kind: ServiceAccount
  name: klap-controller-manager
  namespace: klap-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: klap-metrics-auth-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: klap-metrics-auth-role
subjects:
- kind: ServiceAccount
  name: klap-controller-manager
  namespace: klap-system
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: klap
    control-plane: controller-manager
  name: klap-controller-manager-metrics-service
  namespace: klap-system
spec:
  ports:
  - name: https
    port: 8443
    protocol: TCP
    targetPort: 8443
  selector:
    app.kubernetes.io/name: klap
    control-plane: controller-manager
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: klap
  name: klap-webhook-service
  namespace: klap-system
spec:
  ports:
  - port: 443
    protocol: TCP
    targetPort: 9443
  selector:
    app.kubernetes.io/name: klap
    control-plane: controller-manager
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: klap
    control-plane: controller-manager
  name: klap-controller-manager
  namespace: klap-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: klap
      control-plane: controller-manager
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/default-container: manager
      labels:
        app.kubernetes.io/name: klap
        control-plane: controller-manager
    spec:
      containers:
      - args:
        - --metrics-bind-address=:8443
        - --leader-elect
        - --health-probe-bind-address=:8081
        - --webhook-cert-path=/tmp/k8s-webhook-server/serving-certs
        - --webhook-port=9443
        command:
        - /manager
        image: controller:latest
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8081
          initialDelaySeconds: 15
          periodSeconds: 20
        name: manager
        ports:
        - containerPort: 8081
          name: health
          protocol: TCP
        - containerPort: 9443
          name: webhook-server
          protocol: TCP
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8081
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          limits:
            cpu: 500m
            memory: 128Mi
          requests:
            cpu: 10m
            memory: 64Mi
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: true
        volumeMounts:
        - mountPath: /tmp/k8s-webhook-server/serving-certs
          name: webhook-certs
          readOnly: true
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      serviceAccountName: klap-controller-manager
      terminationGracePeriodSeconds: 10
      volumes:
      - name: webhook-certs
        secret:
          secretName: webhook-server-cert
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: klap
  name: klap-metrics-certs
  namespace: klap-system
spec:
  dnsNames:
  - SERVICE_NAME.SERVICE_NAMESPACE.svc
  - SERVICE_NAME.SERVICE_NAMESPACE.svc.cluster.local
  issuerRef:
    kind: Issuer
    name: klap-selfsigned-issuer
  secretName: metrics-server-cert
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: klap
  name: klap-serving-cert
  namespace: klap-system
spec:
  dnsNames:
  - klap-webhook-service.klap-system.svc
  - klap-webhook-service.klap-system.svc.cluster.local
  issuerRef:
    kind: Issuer
    name: klap-selfsigned-issuer
  secretName: webhook-server-cert
---
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: klap
  name: klap-selfsigned-issuer
  namespace: klap-system
spec:
  selfSigned: {}
---
apiVersion: admissionregistration.k8s.io/v1
kind: MutatingWebhookConfiguration
metadata:
  annotations:
    cert-manager.io/inject-ca-from: klap-system/klap-serving-cert
  name: klap-mutating-webhook-configuration
webhooks:
- admissionReviewVersions:
  - v1
  clientConfig:
    service:
      name: klap-webhook-service
      namespace: klap-system
      path: /mutate-klap-genesary-github-com-v1alpha1-entry
  failurePolicy: Fail
  name: mentry-v1alpha1.kb.io
  rules:
  - apiGroups:
    - klap.genesary.github.com
    apiVersions:
    - v1alpha1
    operations:
    - CREATE
    - UPDATE
    resources:
    - entries
  sideEffects: None
- admissionReviewVersions:
  - v1
  clientConfig:
    service:
      name: klap-webhook-service
      namespace: klap-system
      path: /mutate-klap-genesary-github-com-v1alpha1-server
  failurePolicy: Fail
  name: mserver-v1alpha1.kb.io
  rules:
  - apiGroups:
    - klap.genesary.github.com
    apiVersions:
    - v1alpha1
    operations:
    - CREATE
    - UPDATE
    resources:
    - servers
  sideEffects: None
---
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingWebhookConfiguration
metadata:
  annotations:
    cert-manager.io/inject-ca-from: klap-system/klap-serving-cert
  name: klap-validating-webhook-configuration
webhooks:
- admissionReviewVersions:
  - v1
  clientConfig:
    service:
      name: klap-webhook-service
      namespace: klap-system
      path: /validate-klap-genesary-github-com-v1alpha1-entry
  failurePolicy: Fail
  name: ventry-v1alpha1.kb.io
  rules:
  - apiGroups:
    - klap.genesary.github.com
    apiVersions:
    - v1alpha1
    operations:
    - CREATE
    - UPDATE
    resources:
    - entries
  sideEffects: None
- admissionReviewVersions:
  - v1
  clientConfig:
    service:
      name: klap-webhook-service
      namespace: klap-system
      path: /validate-klap-genesary-github-com-v1alpha1-server
  failurePolicy: Fail
  name: vserver-v1alpha1.kb.io
  rules:
  - apiGroups:
    - klap.genesary.github.com
    apiVersions:
    - v1alpha1
    operations:
    - CREATE
    - UPDATE
    resources:
    - servers
  sideEffects: None
