---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: servers.klap.ripolin.github.com
spec:
  group: klap.ripolin.github.com
  names:
    kind: Server
    listKind: ServerList
    plural: servers
    singular: server
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.url
      name: URL
      type: string
    - jsonPath: .spec.baseDN
      name: BASE_DN
      type: string
    - jsonPath: .spec.implementation
      name: IMPLEMENTATION
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Server is the Schema for the servers API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Server
            properties:
              allowedNamespaces:
                description: |-
                  AllowedNamespaces restricts which namespaces' Entries are allowed to use
                  this Server. When omitted, only Entries from the Server's own namespace may
                  reference it. An Entry located in the same namespace as the Server is always
                  allowed, regardless of this selector.
                properties:
                  labelSelector:
                    description: |-
                      LabelSelector selects namespaces by their labels. When set, any Entry whose
                      namespace carries labels matching the selector is allowed to use the Server.
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                  namePattern:
                    description: |-
                      NamePattern is a regular expression matched against the full name of the
                      Entry's namespace. When set, any Entry whose namespace name matches the
                      pattern is allowed to use the Server.
                    type: string
                type: object
              baseDN:
                description: BaseDN is the base distinguished name for the server
                type: string
              bindDN:
                description: BindDN is the distinguished name to bind as when connecting
                  to the server
                type: string
              implementation:
                default: openldap
                description: Implementation is the server implementation type (e.g.,
                  "openldap", "activedirectory")
                type: string
              passwordSecretRef:
                description: PasswordSecretRef is a reference to a secret containing
                  the password to bind with
                properties:
                  key:
                    description: Key within a secret
                    type: string
                  name:
                    description: Name of a secret
                    type: string
                required:
                - key
                - name
                type: object
              startTLS:
                default: false
                description: StartTLS indicates whether to use StartTLS when connecting
                  to the server
                type: boolean
              timeout:
                default: 30s
                description: |-
                  Timeout is the maximum duration for any operation against the server. When
                  omitted, a default timeout of 30 seconds is used.
                type: string
              tlsSecretRef:
                description: TlsSecretRef is a reference to a secret containing TLS
                  configuration for the server
                properties:
                  key:
                    description: Key within a secret
                    type: string
                  name:
                    description: Name of a secret
                    type: string
                required:
                - key
                - name
                type: object
              url:
                description: Url is the URL of the server
                type: string
            required:
            - baseDN
            - bindDN
            - implementation
            - passwordSecretRef
            - startTLS
            - timeout
            - url
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources: {}
